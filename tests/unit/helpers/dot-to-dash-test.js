import { dotToDash } from '../../../helpers/dot-to-dash';
import { module, test } from 'qunit';

module('Unit | Helper | dot to dash');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dotToDash(42);
  assert.ok(result);
});
